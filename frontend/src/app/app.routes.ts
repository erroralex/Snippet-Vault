import { Routes } from '@angular/router';

/**
 * Defines the main application routes for the Angular router.
 *
 * This configuration holds the routing paths and their associated components
 * or modules, allowing navigation between different views within the single-page
 * application. Currently, it initializes an empty array, ready for future route
 * definitions to be added.
 */
export const routes: Routes = [];
