import {Routes} from '@angular/router';

/**
 * ──────────────────────────────────────────────
 * <h2>routes</h2>
 * ──────────────────────────────────────────────
 * <p><strong>Responsibility:</strong> Defines the main application routes for the Angular router.</p>
 * <p><strong>Functions:</strong></p>
 * <ul>
 * <li>Holds the configuration mapping URL paths to their corresponding components or modules.</li>
 * <li>Enables navigation between different views within the single-page application.</li>
 * <li>Currently initialized as an empty array, serving as a placeholder for future route definitions.</li>
 * </ul>
 * <p><strong>Technical Role:</strong> An Angular {@code Routes} array used by the {@code provideRouter} function during application bootstrapping to set up the router instance.</p>
 * ──────────────────────────────────────────────
 */
export const routes: Routes = [];
